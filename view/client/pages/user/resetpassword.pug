extends ../../layout/default.pug
include ../../mixins/product_layout.pug
include ../../mixins/alert.pug

block title 
    title #{titlePage}


block main
    +alert-danger(5000)
    +alert-success(5000)
    .container.my-5
        .row.justify-content-center
            .col-8
              
                h1(class="text-success") Đặt lại mật khẩu
                form(action="/user/password/resetpassword", method="post") 
                    div(class="form-group")
                        label(for="fullname") Nhập mật khẩu mới
                        input(type="password"
                        class="form-control"
                        name="password"
                        id="password" 
                        )
                    div(class="form-group")
                        label(for="fullname") Nhập lại mật khẩu
                        input(type="password"
                        class="form-control"
                        name="re_password"
                        id="re_password"
                        )
                    div(class="form-group m-2 d-flex justify-content-center")
                        button(class="btn btn-primary" type="submit") Xác nhận


