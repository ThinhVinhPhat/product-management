mixin filterStatus(filterstatus)
     each item in filterStatus
            button(class=`btn btn-sm ml-1 btn-outline-success ${item.class}` style="margin: 10px;" button-status= item.status) #{item.name}