mixin formateDateTime(item)
    p #{moment(item).format('MMMM Do YYYY, h:mm:ss a')}

mixin formateDate(item)
    p #{moment(item).format('MMMM Do YYYY')}

mixin formateTime(item)
    p #{moment(item).format('h:mm:ss a')}
